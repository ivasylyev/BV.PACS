@using BV.PACS.WEB.Shared.Models
@inherits BV.PACS.WEB.Client.Shared.Base.AuditPanel<BV.PACS.WEB.Shared.Models.AliquotAuditGridDto, BV.PACS.WEB.Client.I18nText.AliquotAuditPanel>

@{
    if (GroupedData == null)
    {
        <div class="row">@PacsMessagesTranslations.Loading</div>
    }
    else
    {
        <div class="row">
            <div class="col-sm-12">
                <DxFormLayout CaptionPosition="@CaptionPosition.Vertical">

                    @foreach (var group in GroupedData)
                    {
                        <AliquotAuditPanelRecord 
                            AuditData=@group.Value
                            AuditTitle=@group.Key
                            OnClick=@OnChildClick
                            @key="group.Key">

                        </AliquotAuditPanelRecord>
                    }

                </DxFormLayout>

            </div>
        </div>
    }
}