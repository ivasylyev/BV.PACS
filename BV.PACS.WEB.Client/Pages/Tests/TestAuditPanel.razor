@using BV.PACS.WEB.Shared.Models
@inherits BV.PACS.WEB.Client.Shared.Base.AuditPanel<BV.PACS.WEB.Shared.Models.TestAuditGridDto, BV.PACS.WEB.Client.I18nText.TestAuditPanel>

@{
    if (GroupedData == null)
    {
        <div class="row">@PacsMessagesTranslations.Loading</div>
    }
    else
    {
        <div class="row">
            <div class="col-sm-12">
                <DxFormLayout CaptionPosition="@CaptionPosition.Vertical">

                    @foreach (var group in GroupedData)
                    {
                        <TestAuditPanelRecord 
                            AuditData=@group.Value
                            AuditTitle=@group.Key
                            OnClick=@OnChildClick
@key="group.Key">

                        </TestAuditPanelRecord>
                    }

                </DxFormLayout>

            </div>
        </div>
    }
}