@using BV.PACS.Client.Pages
@using BV.PACS.Client.Pages.Aliquots
@using BV.PACS.Client.Pages.Materials
@using BV.PACS.Client.Pages.Sources
@using BV.PACS.Shared.Models
@using Microsoft.AspNetCore.Components
@{
    if (Context is TestCatalogDto test)
    {
        <BarcodeCell Id=@test.TestId
                          PageRoute=@nameof(TestTracking)
                          Barcode=@test.TestBarcode>
        </BarcodeCell>
    }
}

@functions {

    [Parameter]
    object Context { get; set; }

}