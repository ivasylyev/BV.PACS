@using BV.PACS.Shared.Models
@using Microsoft.AspNetCore.Components
@{

    if (Id > 0)
    {
        <span onclick="@OnClick" class="ml-md-auto">@Barcode</span>
    }
    else
    {
        @Barcode
    }
}

@functions {

    [Parameter]
    object Context { get; set; }

    int Id => Context is SourceCatalogDto source ? source.SourceId : 0;
    string Barcode => Context is SourceCatalogDto source ? source.SourceBarcode : string.Empty;

    [Parameter]
    public Action<int> OnCellClick { get; set; }

    private void OnClick()
    {
        OnCellClick?.Invoke(Id);
    }
}